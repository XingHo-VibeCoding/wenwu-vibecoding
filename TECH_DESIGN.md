# 《电子设备使用指南》技术设计文档（TECH_DESIGN）

> **Day 5 产出** ｜ 2026-09-20 ｜ Vibe Coding 五步工作流 · 第 3 步「设计/技术方案」
>
> **本文档的依据**：`PRD.md` v1.1（Day 4）+ 2026-09-20 的三项拍板（技术路线 / CSS 组织 / 部署方式）。
> **本文档的读者**：Day 6-7 写代码的我、Day 7 上线的我、以后做第二期的我。
> **一句话总纲**：内容对每个人都一样 → 静态站就够了；本项目的"数据"就是 HTML 本身。

---

## 1. 技术路线比较与选型

### 1.1 前端形态

| 方案 | 是什么 | 优点 | 缺点 | 结论 |
|---|---|---|---|---|
| **纯 HTML + CSS 单文件** | 一个 `index.html`，CSS 写在内联 `<style>` 里 | 零依赖、零构建、双击就能打开；发布=上传一个文件；单文件即整站，不会出现"页面和样式对不上版本" | 文件较长（预计 800–1000 行）；改样式要翻同一个文件 | ✅ **采用**（2026-09-20 拍板） |
| HTML + 独立 `style.css` | 结构与样式分两个文件 | 结构清晰，改样式不动 HTML | 多一个文件、多一次请求；与 PRD AC-12"除 index.html 外无其他 HTML"兼容，但部署与分享多一份依赖 | 落选：本期页面只有 1 个，分离收益小于成本 |
| React / Vite 框架 | 组件化开发 + 构建工具链 | 组件复用、状态管理，适合功能多的应用 | 需要Node 环境与构建步骤；产物是"构建后的文件"而非手写 HTML；对"零 JS 约束"直接违约 | 落选：与 PRD 第 3 节约束正面冲突，记入 1.4 未来路线 |

### 1.2 后端

| 方案 | 是什么 | 结论 |
|---|---|---|
| **无后端** | 没有服务器逻辑，内容写死在 HTML 里 | ✅ **采用**。理由：内容对所有人一样（"Android 传文件到 Windows 的正确步骤"只有一个真相），没有"因人而异的数据"，就没有需要服务器记住的东西 |
| CloudBase Node.js 云函数 | 云端跑 Node 代码，响应前端请求 | 落选：无请求可处理。记入 1.4 未来路线（评论/投票上线时启用） |
| 自建服务器（VPS） | 租一台机器自己维护 | 落选：成本与维护量最大，本期没有任何功能需要它 |

### 1.3 数据库

| 方案 | 结论 |
|---|---|
| **无数据库** | ✅ **采用**。本站没有会变化的数据——没有用户、没有评论、没有计数。数据=HTML 里的内容本身 |
| CloudBase PostgreSQL | 落选：没有表可建。记入 1.4 未来路线 |

### 1.4 部署与未来扩展路线

| 方案 | 结论 |
|---|---|
| **GitHub Pages** | ✅ **采用**（2026-09-20 拍板）。理由：与代码仓库一体，`git push` 即更新；零成本；网址形如 `https://xingho-vibecoding.github.io/wenwu-vibecoding/`；Day 7 上线路径最短 |
| CloudBase 静态托管 | 落选：国内访问速度通常更好，但需注册腾讯云 + 实名认证，步骤多；作为备选记录在案 |
| 暂不部署 | 落选：PRD AC-01 只要求本地可打开，但 Day 7 任务清单要求上线，GitHub Pages 无额外成本 |

> **未来扩展路线（第二期留档，本期不实施）**：React/Vite + CloudBase Node.js 云函数 + CloudBase PostgreSQL + 静态托管。触发条件：上线评论/投票（需要后端存数据）、扩展教学仪器方向（内容结构化后需要按设备筛选）。届时先做数据迁移评估，见第 7 节。

---

## 2. 项目结构

```
wenwu-vibecoding/
├── index.html        # 整站唯一页面：页头（任务跳转按钮+速查）→ S1–S4 场景板块 → 资源入口 → 页脚
├── research.md       # Day 3 需求研究（文档，非代码）
├── PRD.md            # Day 4 需求文档（文档，非代码）
├── TECH_DESIGN.md    # 本文档
├── .gitignore        # 排除名单（.env、.workbuddy/ 等，Day 2 建）
├── AGENTS.md         # 协作规则（文档，非代码）
└── .env              # 本地私有文件，忽略规则已拦截，本期为空占位
```

要点：

- **没有 `js/`、`css/`、`assets/` 目录**——单文件策略的直接结果。截图、示意图如未来需要，才引入 `assets/`，本期内容纯文字。
- `index.html` 内部按注释分节：`<!-- S1 场景板块 -->` 等，方便 Day 6 定位修改。
- `.git/`、`.workbuddy/` 为本地目录，不入库（前者是 Git 底账，后者是工作记录）。

---

## 3. 数据模型

本项目没有数据库，"数据模型"落地为 **HTML 中的内容结构约定**（与 PRD 第 6 节字段一一对应）。Day 6 写内容时按此填，验收按此查。

**场景级**（每场景一个 `<section id="scene-N">`）：

| 字段 | HTML 落点 |
|---|---|
| `scene_id` | `section` 的 `id` 属性（`scene-1` … `scene-4`） |
| 场景名 | 板块标题（`<h2>`），同时作为 F1a 任务按钮的跳转目标 |
| 一句话描述 | 标题下方说明行 |
| 适用设备/系统 | 固定标注"Android + Windows"（标签样式） |
| 前置条件 | 场景信息区的"前置条件"条目 |
| 总耗时 | 场景信息区的"总耗时"条目 |
| 步骤列表 | 有序列表 `<ol>`，每步一条 |

**步骤级**（`<ol>` 内每一项）：

| 字段 | HTML 落点 |
|---|---|
| 步骤描述 | 列表项正文 |
| 难度星级（1–5） | 列表项内的星级标注（视觉形式由本文档第 6 节定） |
| 耗时（分钟） | 列表项内的耗时标注 |
| 注意事项 | 可选，列表项内的"坑"提示 |

**资源级**（F2 板块每条链接）：名称 / `URL`（`<a href>`）/ 用途一句话 / 类型（官方或社区）。

**任务按钮条目**（F1a，v1.2）：每个按钮 = 一个 `<form action="#scene-N"><button>`，按钮文本 = 场景名或别名，与 `scene-N` 一一对应。（v1.1 及之前为 `<datalist>` 搜索框，Day 7 实测证实 datalist 选中无法跳转，已按 PRD v1.2 变更）

---

## 4. API 列表

**本项目无任何 API**。没有后端就没有接口；页面所需的全部信息在 HTML 加载时已完整到达浏览器。

PRD 中"跳转到场景"的交互，由**锚点**承担"接口"角色：

| 锚点 | 作用 | 等价的"接口语义" |
|---|---|---|
| `#scene-1` … `#scene-4` | 速查链接跳转到对应场景板块 | 按场景 ID 查询 |
| `#resources` | 跳转到资源入口板块 | 查询资源列表 |

浏览器原生解析 URL 锚点并滚动定位——这就是"零 JS 跳转"的全部实现，不需要写一行代码。

---

## 5. 前后端数据流

没有后端，数据流只剩两端，但"数据从哪来、到哪去"仍然成立：

```
内容来源（我，Day 6 实测后手写）
        │  git push
        ▼
GitHub 仓库（main 分支，唯一数据源）
        │  GitHub Pages 自动发布
        ▼
访问者浏览器（加载 index.html，一次性拿到全部内容）
        │  交互：任务按钮跳转 / 锚点跳转（均由浏览器原生能力完成）
        ▼
用户眼睛（读，仅此而已——本站不收集、不回传任何数据）
```

- **数据只有一个来源**：仓库里的 `index.html`。没有第二份数据，就没有"两边不一致"的问题。
- **数据只有一个去向**：用户的浏览器渲染出来。没有任何数据从用户流回服务器（F1a 任务按钮虽是 `<form>`，但 action 只是页内锚点、不含任何输入字段，仅触发跳转、不提交数据；没有埋点）。
- mermaid 版（GitHub 可直接渲染，Day 5 截图用）：

```mermaid
flowchart LR
    A[我：Day 6 实测并手写内容] -->|git push| B[GitHub 仓库 main 分支]
    B -->|GitHub Pages 自动发布| C[访问者浏览器]
    C -->|加载 index.html 一次性到齐| D[用户阅读]
    C -.->|无任何回传| B
```

---

## 6. 错误处理

静态站没有运行时服务可崩，错误只来自四个地方，逐一给处理方式：

| 错误来源 | 表现 | 处理方式 |
|---|---|---|
| 内容错误（步骤写错、难度/耗时标错） | 用户照做失败 | ① Day 6 实测制度：每步实测后再标注，来不及实测的整条标"**待实测**"（PRD 8.3）；② 页脚放"发现错误怎么办"指引；③ 修正走正常 git 提交流程，push 即生效 |
| 链接失效（F2 外部官网改版） | 点击后 404 | ① 只收录大厂官方域名，降低失效概率；② 资源链接集中放在第 ③ 区块，修一处即修全部 |
| 锚点失效（id 改名忘同步） | 速查链接点了没反应 | 约定：`scene-N` 的 id 与 F1b 链接在同一文件内，Day 6 自查清单含"4 个锚点逐个点击验证"（对应 AC-06） |
| 部署未生效（push 了但页面没更新） | 网页还是旧版 | GitHub Pages 构建有 1–3 分钟延迟，先等再查；超过 5 分钟查仓库 Settings → Pages 的部署记录 |

**兜底原则**：本站最大的"错误处理"是**断网也能用**（PRD AC-03）——内容全在 HTML 里，没有网络请求就没有网络错误。

---

## 7. 环境变量

**本期没有任何环境变量。** 没有后端、没有 API 调用、没有第三方服务，就没有需要隐藏的密钥。

- 仓库里的 `.env` 是 Day 2 建的占位文件（内容仅注释），已被 `.gitignore` 拦截，**本期保持为空**，第 23 天（接入数据库配置时）才启用。
- 这一条写在这里的意义：把"环境变量"从"不知道要不要"变成"明确为无"，第二期加后端时照 PRD/本文档的迁移注意重新评估。

---

## 8. 迁移注意事项（写给第二期的自己）

本期选了最轻的路线，代价是把扩展性欠了账。第二期升级（加评论/投票/仪器方向）时，逐条核对：

1. **内容迁移**：现在内容写死在 HTML 里，届时需要先**把内容抽成结构化数据**（JSON 或数据库表）。第 3 节的字段约定就是为这一天准备的——只要 Day 6 严格遵守字段结构，抽取就是机械活。
2. **难度/耗时数据**：这些是本项目最值钱的自有数据，迁移时**不能丢**，且每条都带"实测/待实测"状态，入库时要保留该状态。
3. **URL 与锚点**：`#scene-N` 已是事实上的公开地址（用户可能收藏）。升级后保持锚点兼容，或做跳转。
4. **技术栈跃迁**：单文件 HTML → React/Vite 意味着引入 Node 构建链，本地环境要装 Node.js；届时 git 工作流不变，但"push 即上线"变为"push 触发构建"。
5. **成本意识**：加后端 = 从"零成本"进入"可能产生云服务费用"，届时需重新评估 PRD 的暂不做清单（哪些功能值得花这个钱）。

---

## 9. 给 Day 6（开发）的输入

1. **单文件策略**：全部代码进 `index.html`，CSS 内联 `<style>`，禁止引入 `<script>`（PRD AC-02 一票否决）。
2. **字段即结构**：第 3 节的 HTML 落点就是写内容的模板，字段一个不少（AC-07/08/09 按此验收）。
3. **难度/耗时必须实测**（PRD 8.3），星级视觉形式建议：★☆ 字符 + 耗时数字，颜色区分难度档（具体 Day 7 按设计微调）。
4. **自查顺序**：AC-01（本地打开）→ AC-02（无 script）→ AC-03（断网）→ AC-04~06（跳转按钮与锚点）→ AC-07~09（场景结构）→ AC-10（资源链接）→ AC-11（无越界功能）→ AC-12（单 HTML 文件）。

---

## 10. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-20 | 初稿（板块②产出）。技术路线按 2026-09-20 三项拍板定稿：零后端静态路线 / CSS 内联单文件 / GitHub Pages 部署 |
| v1.1 | 2026-09-21 | F1a 落地形态变更（随 PRD v1.2）：datalist 搜索框 → 8 个 `<form action="#scene-N"><button>` 任务跳转按钮（零 JS 保留）。同步修改：第 2 节项目结构注释、第 3 节数据模型（任务按钮条目）、第 5 节数据流图与说明、第 9 节自查顺序 |
