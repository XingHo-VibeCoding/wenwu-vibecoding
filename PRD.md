# 《电子设备使用指南》产品需求文档（PRD）

> **Day 4 产出** ｜ 2026-09-19 ｜ Vibe Coding 五步工作流 · 第 2 步「需求定义」
>
> **本文档的依据**：`research.md`（Day 3）+ 2026-09-19 的四项拍板。
> **本文档的读者**：Day 5 做设计的我、Day 6-7 写代码的我、板块③自检的我。
> **判卷标准**：第 9 节「验收标准」。任何关于"做没做到"的争论，以那组编号为准。

---

## 1. 问题定义

**大二大三理工学生在手机（Android）和电脑（Windows）之间流转资料时，面对多种传法（数据线、聊天工具、局域网、网盘），不知道哪种适合自己当前的场景、分几步、要多久、难不难——网上没有一份按场景组织、且每一步都标注难度与耗时的现成指南。**

本作品是一份静态网页指南，按"场景"组织，让用户在动手前就知道：这件事要分几步、每步多难、要花多久。

## 2. 目标用户与场景

| 项 | 内容 |
|---|---|
| **目标用户** | **大二大三理工科学生**（2026-09-19 变更，原"新入学大学生"，见 `research.md` 修订记录） |
| **设备组合** | **Android 手机 + Windows 电脑**（本期固定，每个步骤只写这一套做法） |
| **典型场景** | 实验课拍了数据/板书要传到电脑写报告；报告写完要在手机上过一眼；实验视频、安装包等大文件聊天工具传不动；代码和学习资料两端来回倒 |
| **他们的共同状态** | 不怕用软件，但**没耐心试错**——查一个传法要试半小时，试错了也不知道哪一步出问题 |

## 3. 实现约束（已拍板，Day 5 不再讨论）

| 约束 | 内容 | 来源 |
|---|---|---|
| 形态 | **单页静态站点**（`index.html`），页内锚点分区 | Day 3 |
| 技术 | 单文件静态站：HTML + 内联 CSS + **一段内联 JS（本地 mock 渲染用）**，零后端、零网络请求（v1.3 修订：v1.0–v1.2 为"零 JS"，Day 8 因四状态 mock 渲染需求变更） | Day 3 拍板，Day 8 v1.3 变更 |
| 任务跳转 | 用纯 HTML 表单按钮实现"按任务跳场景"（详见 F1a；v1.2 由 datalist 搜索框变更而来） | Day 4 拍板，Day 7 v1.2 变更 |
| 设备 | 只覆盖 Android + Windows，每步骤一套做法 | Day 4 拍板 |

> 注意：这条是**用户已拍板的约束**，不是本 PRD 要做的技术选型。CSS 内联还是独立文件等实现细节，留给 Day 5。

## 4. 功能清单（每条 = 用户动作 + 可观察结果）

### F1a 任务跳转按钮组（v1.2 由"搜索输入框 datalist 轻量版"变更而来）

- **用户动作**：点击页头任一任务按钮（如"拍板书""传安装包"）
- **可观察结果**：页面滚动到该任务对应的场景板块，地址栏 URL 末尾出现 `#scene-N`
- **边界（本期不承诺）**：只覆盖**预登记任务条目**（场景名 + 别名，共 8 个按钮），不提供关键字过滤输入
- **变更原因**：Day 7 用户浏览器实测证实，`<datalist>` 选中条目后原生行为仅是"把候选词填进输入框"，无任何执行动作的能力；纯 HTML 下无法实现搜索式跳转。经用户在三个方案中拍板（2026-09-21）改为任务按钮组——零 JS 保留，跳转能力反而补齐

### F1b 场景速查链接

- **用户动作**：点击任务按钮下方的场景速查链接（每个场景一条，常驻显示）
- **可观察结果**：页面滚动到对应场景板块，地址栏 URL 末尾出现 `#scene-N`

### F2 官方 / 实用资源入口

- **用户动作**：滚动到"资源入口"板块 → 浏览链接列表 → 点击某条
- **可观察结果**：链接在**新标签页**打开对应官网；每条链接旁有一句话说明"点它能干什么"
- **内容要求**：只收录**官方或明确署名**的站点（如 Android 官方支持页、Windows 官方帮助页、常用工具官网）；每条链接的用途说明必须具体，不写"官网"两个字应付

### F3 场景板块（核心功能）

- **用户动作**：从速查链接或滚动进入某个场景板块 → 按"这是什么 → 前置条件 → 分步清单"逐条照做
- **可观察结果**：
  - 每个场景开头能看到：场景名、一句话描述、**适用设备/系统标注**（设备信息融进场景页）、前置条件、总耗时
  - 清单中**每一步**都能看到：做什么、**难度星级（1–5）**、**预计耗时（分钟）**
  - 做完一步能明确看到下一步是什么

**本期场景清单（4 个，固定）**：

| 编号 | 场景 | 主题 |
|---|---|---|
| S1 | 实验数据 / 板书传电脑 | 手机拍的照片怎么进电脑、进报告 |
| S2 | 电脑文件传回手机 | 报告/资料在手机上预览、提交前检查 |
| S3 | 大文件传输 | 超出聊天工具限制的文件（实验视频、安装包）怎么传 |
| S4 | 资料两端同步 | 代码、笔记等学习资料的日常同步 |

> 场景内具体写哪种方法（数据线/聊天工具/局域网/网盘）属于内容工作，Day 6 定；PRD 只框定主题和结构要求。

### F5 仪器速查搜索（v1.4 新增，2026-09-26 拍板：甲 + 方案 A + 操作级）

- **用户动作**：滚动到"仪器速查"板块 → 在搜索框输入关键词（如"通断""限流""正弦波"）→ 查看匹配的仪器操作条目
- **可观察结果**：页面**实时过滤**出匹配条目（每条含：仪器名 + 操作名 + 分步清单 + 出处页码）；无匹配时显示空态提示；清空输入恢复全部条目
- **搜索口径**：**本地关键词匹配**——对预登记条目的"操作名 + 触发关键词"做包含匹配（不区分大小写），不是全文搜索、不是 AI 问答，零网络请求
- **内容口径**：**34 条操作条目、覆盖 6 台仪器**（直流稳压电源 GPS-2303C / 数字万用表 GDM-8341 / 数字示波器 GDS-1102B / 信号发生器 AFG-2225 / LCR 测试仪 LCR-6002 / 晶体管图示仪 WQ4830），从厂商说明书 PDF 提炼**常用操作**（操作级，不抄全书），每条标注出处页码便于回查
- **PDF 原件处理**：说明书 PDF（约 24MB）**不进入仓库、不部署**——体积会撑爆 git，且涉及厂商版权；原件仅本地留存，仓库只收录提炼后的文字条目
- **边界（本期不承诺）**：不做分词 / 同义词 / 模糊匹配；不覆盖说明书全部内容；不带仪器原理讲解

## 5. 页面 / 视图列表

**本期只有 1 个页面**：`index.html`，自上而下 6 个区块：

| 区块 | 内容 | 承载功能 |
|---|---|---|
| ① 页头 | 站点名 + 一句话定位 + **任务跳转按钮组（F1a）** + **场景速查（F1b）** | F1a、F1b |
| ①b 主视图（Day 8 新增） | "今天要解决什么？"+ **场景卡片网格 + 资源卡片网格（mock 数据渲染）** + 四状态演示（加载中/成功/空/错误） | F3 摘要视图 |
| ② 场景板块 ×4 | S1–S4，每块含场景信息 + 分步清单 | F3 |
| ③ 资源入口 | 官方/实用链接列表 | F2 |
| ④ 仪器速查（v1.4 新增） | **搜索输入框 + 仪器操作条目列表**（默认全部显示，输入关键词实时过滤） | F5 |
| ⑤ 页脚 | 更新日期 + "卡住了找谁"指引 + 免责说明 | — |

## 6. 数据字段

**场景级**（每个场景一份）：

| 字段 | 说明 | 示例 |
|---|---|---|
| `scene_id` | 锚点标识 | `scene-1` |
| `场景名` | 显示名，含搜索别名 | 实验数据传电脑 |
| `一句话描述` | 这个场景解决什么 | 拍的板书和数据进电脑写报告 |
| `适用设备/系统` | 固定标注 | Android + Windows |
| `前置条件` | 需要提前准备什么 | USB 数据线 / 同一 Wi-Fi / 指定 App |
| `总耗时` | 各步骤耗时之和 | 约 5 分钟 |
| `步骤列表` | 见下 | — |

**步骤级**（场景内每步一条）：

| 字段 | 说明 |
|---|---|
| `步骤描述` | 做什么，一步只说一件事 |
| `难度星级` | 1–5 星，**必须实测后标注，不准编** |
| `耗时` | 分钟数，同上 |
| `注意事项` | 可选。常见的坑、失败时先查什么 |

**资源级**（F2 每条链接）：`名称` / `URL` / `用途一句话` / `类型（官方或社区）`

**任务按钮条目**（F1a 的按钮文本）：`显示名（场景名或别名）`，与场景锚点一一对应

**仪器操作条目**（F5，v1.4 每条一份）：

| 字段 | 说明 | 示例 |
|---|---|---|
| `仪器名` | 仪器类型 + 型号 | 示波器 GDS-1102B |
| `操作名` | 这条教什么，一句话 | 通断测试（查线路/焊点） |
| `触发关键词` | 搜索匹配用的词表（含别名、英文小写） | 通断 / 短路检查 / 蜂鸣 / 断线 |
| `步骤列表` | 分步清单，一步一件事，写法同场景步骤 | 按两次 Ω 键进入连续性测试… |
| `出处` | 说明书名称 + PDF 实际页码 | 操作手册 P48 |

## 7. 暂不做清单（后续）

| 不做的功能 | 为什么 / 后续 |
|---|---|
| 评论、投票、用户投稿 | 需要后端 + 账号体系 + 内容审核。**下一期方向**（来自 Day 3 研究的核心收获） |
| 收藏 / 学习进度 | 纯前端存本地，换设备即丢，造成"数据假象" |
| 会员 / 付费内容 | 无内容量与用户量支撑 |
| 移动端专门适配 | 本期只保证电脑端正常，手机能凑合看 |
| 教学仪器（示波器等）**完整指南** | v1.4 起**速查版已纳入本期（F5）**；完整指南仍不做——说明书全文内容量大、逐台搬进页面维护成本高，F5 只收录常用操作（操作级） |
| 后台管理系统 | 内容直接写在 HTML 里 |
| 学校流程类内容 | 官方已有权威版本，必要时用 F2 链接指向 |
| 账号注册 / 登录 | 本站无需任何登录 |
| 真·全文搜索（说明书全文检索） | v1.4 后仍不做：F5 搜索对象是**预登记的 34 条速查条目**，不是说明书全文；全文检索涉及大数据量与分词，超出本期范围 |
| 关键字过滤输入框 | datalist 选中条目只会"填词"不会"执行动作"（Day 7 实测证实），v1.2 改为按钮组后不再有输入框 |
| 多设备组合（iOS / Mac / iPad） | 本期只做 Android + Windows，其他组合列为后续 |

## 8. 已知限制与风险

1. **纯 HTML 搜索式跳转做不到**（v1.2 定案）：`<datalist>` 选中条目只会把候选词填进输入框，浏览器不提供"选中后执行动作"的原生事件，除非引入 JS。Day 7 用户实测复现此行为后，F1a 由 datalist 搜索框变更为任务跳转按钮组。
2. **"选中即跳转"在纯 HTML 下做不到**（写 PRD 时确认的技术事实，修正了 Day 4 早晨"零 JS 可行"的不准确说法）。F1b 速查链接是跳转的正式路径。
3. **难度星级与耗时必须实测**：Day 6 内容阶段逐条实测后填入；来不及实测的步骤**整条标注"待实测"**，宁缺不编。
4. **单页滚动较长**：靠 F1b 速查与锚点缓解，不做"回到顶部"以外的增强。

## 9. 验收标准（可逐条判定）

> 判定方式：每条独立成立记 ✅，任一条不成立即整体未通过。

| 编号 | 验收标准 |
|---|---|
| AC-01 | 双击 `index.html` 能在浏览器中直接打开并完整显示，无报错提示 |
| AC-02 | 查看页面源代码：**无外部 JS 引用、无任何网络请求**（内联 mock 脚本允许；v1.3 修订，原"不含 script 标签"随 Day 8 mock 渲染变更） |
| AC-03 | 断开网络后重新打开页面：内容完整显示，无空白区块、无加载失败占位 |
| AC-04 | 页头存在任务跳转按钮组，共 8 个按钮，4 个场景每个至少对应 2 条（场景名 + 任务别名） |
| AC-05 | 点击任一任务按钮：地址栏 URL 末尾出现对应 `#scene-N`，且页面滚动到该场景板块 |
| AC-06 | 点击任一场景速查链接：地址栏 URL 末尾出现 `#scene-N`，且页面滚动到对应板块 |
| AC-07 | 页面恰有 4 个场景板块（S1–S4），每个含：场景名、一句话描述、适用设备/系统标注、分步清单 |
| AC-08 | 每个步骤都带难度星级（1–5）和耗时（分钟），无一步缺失；来不及实测的按第 8.3 条标注"待实测" |
| AC-09 | 每个场景标注前置条件 |
| AC-10 | 资源入口板块 ≥ 5 条链接，每条含名称、网址、一句用途说明；**联网状态下**点击后在新标签页打开 |
| AC-11 | 全站无登录入口、无评论区、无投票、无后台链接（否决式验收：不该有的出现了即不通过）。注：v1.4 起"教学仪器速查"（F5）已纳入本期，不再是越界项；但**说明书全文不得整体搬入**，页面只允许出现 F5 提炼条目 |
| AC-12 | 除 `index.html` 外无其他 HTML 文件（CSS 等样式文件数量不限）；全部内容在 `index.html` 内通过滚动或锚点可达 |
| AC-13 | 主视图（Day 8 新增）能呈现**加载中 / 成功 / 空 / 错误**四种状态：默认打开经历"骨架屏→成功"；点演示按钮或加 `?state=empty`、`?state=error` 可复现空态与错误态；错误态有重试按钮；场景与资源卡片由本地 mock 数据渲染，数据为零网络请求 |
| AC-14 | 仪器速查板块（v1.4 新增）：① 板块内有搜索输入框，默认显示全部 34 条操作条目，覆盖 6 台仪器；② 输入关键词（如"通断"）后条目列表**实时过滤**出匹配项，每条可见仪器名、操作名、分步清单、出处页码；③ 输入无匹配词显示空态提示（不是静默空白）；④ **断网状态下**搜索功能照常可用（本地匹配，零网络请求） |

---

## 11. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-19 | 初稿（板块②产出） |
| v1.1 | 2026-09-19 | 板块③自检修订：AC-04 消除与数据字段"别名"的矛盾；AC-12 消除"前端页面文件是否含 CSS"的歧义；AC-10 补充联网前提；新增本修订记录 |
| v1.2 | 2026-09-21 | **F1a 变更**：Day 7 用户浏览器实测证实 datalist 选中无法跳转（纯 HTML 做不到搜索式跳转），经用户三选一拍板，F1a 由"搜索输入框 datalist 轻量版"变更为"任务跳转按钮组"（8 个 `<form>`+`<button>`，零 JS）。同步修改：第 3 节约束表、第 5 节页头结构、第 6 节数据字段、第 7 节暂不做清单、第 8 节已知限制、AC-04 / AC-05（注：本行补登于 Day 8——当日编辑工具漏写，2026-09-22 复查发现） |
| v1.3 | 2026-09-22 | **技术路线变更（Day 8 拍板，方案 A）**：引入内联 JS + 本地 mock 数据渲染主视图（加载中/成功/空/错误四状态），守"零后端、零网络请求"，"零 JS"口径废止。同步修改：第 3 节技术约束、第 5 节区块表（新增 ①b 主视图）、AC-02 改口径、新增 AC-13 |
| v1.4 | 2026-09-26 | **教学仪器速查纳入本期（用户三选一拍板：甲改 PRD + 方案 A 内容提炼进页面 + 操作级）**：新增 F5 仪器速查搜索——34 条操作条目（6 份厂商说明书提炼，每条标出处页码）+ 本地关键词搜索框（实时过滤，零网络请求）；PDF 原件不进仓库。同步修改：第 5 节区块表（新增 ④ 仪器速查，页脚顺延为 ⑤）、第 6 节数据字段（仪器操作条目）、第 7 节（教学仪器/真·全文搜索口径更新）、AC-11（教学仪器移出否决项）、新增 AC-14 |

---

## 10. 给 Day 5（设计）的输入

1. **页面结构已定**：第 5 节的 4 个区块顺序就是页面的纵向结构。
2. **必须保留的亮点**：AC-08 的"每步难度 + 耗时"是本项目差异点，设计稿里要给它**视觉位置**（不是挤在正文里的小字）。
3. **待设计决定**：视觉风格、难度星级的呈现方式（星图/色块/数字）、CSS 组织方式——属于技术/设计选型，PRD 不预设立场。
